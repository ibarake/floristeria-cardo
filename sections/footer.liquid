<footer>
  <item class="info-marca">
    <img src="{{ settings.logo.src | img_url }}"alt="{{ settings.logo.alt }}">
    <p class="parrafo-1">{{ section.settings.descripcion-marca }}</p>
  </item>
  <item class="menu-marca">
    <h5 class="titulo-5">{{ section.settings.titulo-menu }}</h5>
      <ul>
        {% for link in section.settings.menu-footer.links %}
        <li>
          <a href="{{ link.url }}" class="subtitulo-3">{{ link.title }}</a>
        </li>
        {% endfor %}
      </ul>
  </item>
  <item class="contact">
    <form>
      <input type="text">
      <label for=""></label>
      <button></button>
    </form>
  </item>
</footer>
{% schema %}
  {
    "name": "Custom footer",
    "settings": [
      {
        "type": "textarea",
        "id": "descripcion-marca",
        "label": "Descripción",
        "default": "Descripción"
      }, {
        "type": "text",
        "id": "titulo-menu",
        "default": "Titulo",
        "label": "Título"
      }, {
        "type": "link_list",
        "id": "menu-footer",
        "default": "footer",
        "label": "Selecciona el menú"
      }
    ]
  }
{% endschema %}
</footer>
